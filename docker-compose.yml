services:
  tunnel:
    container_name: tunnel  # This fixes the "No such container" error
    image: cloudflare/cloudflared:latest
    restart: always
    environment:
      - TUNNEL_TOKEN=${CF_TUNNEL_TOKEN}
    command: tunnel run
    networks:
      - factory_net

  global_nginx:
    container_name: global_nginx
    image: nginx:alpine
    restart: always
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./www:/var/www:ro
    networks:
      - factory_net

networks:
  factory_net:
    driver: bridge
