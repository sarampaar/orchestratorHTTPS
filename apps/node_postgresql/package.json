{
  "name": "node_postgresql",
  "version": "1.0.0",
  "description": "Mockup Node.js app with Prisma",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "prestart": "npx prisma generate && npx prisma db push --accept-data-loss"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "express": "^4.18.2",
    "prisma": "^5.0.0"
  }
}
